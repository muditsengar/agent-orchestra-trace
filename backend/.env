# OpenAI API Key (Required)
# Replace with your actual API key before use
OPENAI_API_KEY=sk-proj-xxxx-placeholder-key-for-github-xxxx

# Model Configuration
DEFAULT_MODEL=gpt-4o-mini  # Using GPT-4o mini
FAST_MODEL=gpt-4o-mini  # Using GPT-4o mini for all cases
TEMPERATURE=0.7  # Lower temperature for more focused responses
MAX_TOKENS=1000  # Limit token usage per request
FREQUENCY_PENALTY=0.5  # Reduce repetition
PRESENCE_PENALTY=0.3  # Encourage focused responses

# Rate Limiting Configuration
REQUESTS_PER_MINUTE=50
TOKENS_PER_MINUTE=40000
COOLDOWN_PERIOD=60  # seconds

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=True

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
